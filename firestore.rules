rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Regra simplificada para permitir leitura e escrita em toda a base,
    // uma vez que a autenticação agora é controlada pela própria aplicação.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
