
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Regra Padrão: Negar leitura e escrita para todos por padrão.
    // Acesso só é concedido se uma regra mais específica abaixo permitir.
    match /{document=**} {
      allow read, write: if false;
    }

    // A coleção 'authorizedUsers' pode ser lida por qualquer pessoa para
    // verificar a autorização de e-mail antes do login completo.
    // A escrita é bloqueada para todos para evitar adulteração.
    match /authorizedUsers/{userId} {
      allow read: if true;
      allow write: if request.auth != null; // Apenas autenticados (admins) podem escrever
    }

    // A coleção 'collaborators' contém dados sensíveis e só pode ser
    // lida ou escrita por usuários que já estão autenticados na aplicação.
    match /collaborators/{collaboratorId} {
      allow read, write: if request.auth != null;
    }

    // Para todas as outras coleções, apenas usuários autenticados (logados)
    // podem ler ou escrever dados.
    match /businessAreas/{areaId} {
      allow read, write: if request.auth != null;
    }
    match /initiatives/{initiativeId} {
      allow read, write: if request.auth != null;
    }
    match /kpis/{kpiId} {
      allow read, write: if request.auth != null;
    }
    match /okrs/{okrId} {
      allow read, write: if request.auth != null;
    }
    match /recurringMeetings/{meetingId} {
      allow read, write: if request.auth != null;
    }
     match /settings/{settingId} {
      allow read, write: if request.auth != null;
    }

    // Coleções específicas do usuário (Notas e Tarefas)
    // Apenas o próprio usuário pode acessar seus dados.
    match /notes/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    match /tasks/{taskId} {
       allow read, write: if request.auth != null && request.auth.uid == resource.data.userId;
    }
    match /auditLogs/{logId} {
        allow read, write: if request.auth != null;
    }
  }
}
